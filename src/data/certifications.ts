import type { Certification } from '../types/portfolio';

// No certification facts are proven in the current repository source.
// Create the architecture now; data will be populated when verified.
export const CERTIFICATIONS: Certification[] = [];
